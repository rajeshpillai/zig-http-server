# Zig Server
A simple Zig server


# Usage
To run the app

```
zig run src/self-serve -- public
```


# To build the app
```
zig build

```


# Run the build (serve from public folder)
```
./zig-out/bin/self-serve public
```